---
- hosts: all
  become: true

  tasks:
    - name: Atualizar cache
      apt:
        update_cache: true

    - name: Instalar pacotes base
      apt:
        name:
          - vim
          - curl
          - wget
          - sudo
          - rsync
          - python3
        state: present
