all:
  children:
    gateway:
      hosts:
        gateway:
          ansible_host: 10.0.0.2
    auth:
      hosts:
        auth:
          ansible_host: 10.0.0.3
    test:
      hosts:
        test:
          ansible_host: 10.0.0.10
